from django.apps import AppConfig


class JournalActiviteConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.journal_activite"
    label = "journal_activite"
    verbose_name = "Journal d'activité"
